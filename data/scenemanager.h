#ifndef SCENEMANAGER_H
#define SCENEMANAGER_H

class SceneManager
{
public:
    /*SceneManager();

    // Load all game levels
    void loadScenes();

    // Update game level according to distance cut by player
    void updateScene(int distance);

    // Gets the number of obstecles in the current level
    int getObsteclesCount() const;

    // Gets the number of bullets updates in the current level
    int getBulletsUpdate() const;

    // Gets the number of monsters in the current level
    int getMonstersCount() const;

private:
    //number of obstecles in current level(scene)
    int obsteclesCount;

    //number of bullets in current level(scene)
    int bulletsUpdate;

    //number of monsters in current level(scene)
    int monstersCount;

    //speed of movment of enemies
    int enemySpeed;

    //max distance of current level
    int maxDistance;    */
};

#endif // SCENEMANAGER_H
